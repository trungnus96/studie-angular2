import { GroupDegreeBySchoolPipe } from './group-degree-by-school.pipe';

describe('GroupDegreeBySchoolPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupDegreeBySchoolPipe();
    expect(pipe).toBeTruthy();
  });
});
